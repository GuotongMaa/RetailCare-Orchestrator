# Open Questions

Use this file to preserve questions across conversations. Move resolved items
into session notes or `progress.md`.

## Current Questions

- How exactly does LangGraph merge `messages` across nodes using
  `operator.add`?
- What is the difference between the agent loop, a workflow, and an eval
  harness in day-to-day engineering terms?
- Where should a new tool be added so it works in the agent, MCP server, tests,
  and evals?
- Why is this project single-agent first, and what evidence would justify
  splitting into multiple agents?
- Are `src/retailcare/data/models 2.py` and `src/retailcare/data/db 2.py`
  intentional backups or accidental duplicates?

## Resolved Questions

None yet.
