# Experiments

Use this folder for prototypes that may later be promoted into the main
RetailCare architecture.

Each experiment should include:

- goal;
- files inspected;
- prototype files;
- observed result;
- promotion recommendation: `promote`, `keep learning-only`, or `discard`.

Promotion must follow `docs/learning/promotion-policy.md`.
